{
  "editable": true,
  "panels": [
    {
      "title": "Failed Authentication & Unauthorized 401/403 Responses",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "sum(rate(http_requests_total{status=~\"401|403\"}[1m])) or vector(0)",
          "legendFormat": "Auth Failures / sec",
          "refId": "A"
        }
      ]
    },
    {
      "title": "Automated Loop Guard Blocked Actions (Escalations)",
      "type": "stat",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "vector(0)",
          "legendFormat": "Safety Guard Blocks",
          "refId": "A"
        }
      ]
    }
  ],
  "refresh": "5s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["operations", "security"],
  "time": { "from": "now-15m", "to": "now" },
  "title": "Security & Access Audit",
  "uid": "security-audit-dash",
  "version": 1
}
